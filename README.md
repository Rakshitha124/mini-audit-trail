# mini-audit-trail
A full-stack Next.js app that tracks text changes. Users enter content, save versions, and view a history showing added/removed words, text length changes, timestamps, and UUIDs. A custom diff algorithm powers version comparison, stored in-memory for fast audit trail generation.
